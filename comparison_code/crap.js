/*
if (lastName === "Jr" || lastName === ".Jr")
{
  for (i = 0; i < name.length && name.charAt(i) !== " "; i++){
  }
  var start = i;
  for (i = name.length - 1; i >= 0 && name.charAt(i) !== " "; i--){
  }
  var end = i;
  lastName = name.substring(start + 1, end);
}
*/
